export class Constants {
   static TIMELINE_SLOT_MINUTES = 30;
   static EXTENDED_SCHEDULED_PILL_MINUTES = this.TIMELINE_SLOT_MINUTES;
   static DETAIL_SEPARATOR = ' \u2022 ';
   // Keep in sync with styles/tokens.css (--itinerary-half-hour-slot-height, etc.).
   static TIMELINE_SLOT_HEIGHT_PX = 730;
   static TIMELINE_PILL_STRIP_TOP_OFFSET_PX = 80;
   static TIMELINE_SCHEDULED_PILL_MIN_CLUSTER_MINUTES = 3;
   static TIMELINE_SCHEDULED_PILL_MIN_CLUSTER_HEIGHT_PX = Math.round(
      (this.TIMELINE_SCHEDULED_PILL_MIN_CLUSTER_MINUTES / this.TIMELINE_SLOT_MINUTES)
         * this.TIMELINE_SLOT_HEIGHT_PX
   );
   static TIMELINE_POINT_PILL_HEIGHT_PX = 153;
   static MAX_TIMELINE_PILL_INDIVIDUAL_COLUMNS = 2;
   static MAX_TIMELINE_PILL_COLUMNS = 2;
   static DAY_PLANNER_ACTION_FEEDBACK_DISMISS_MS = 2500;
   static DAY_PLANNER_ACTION_FEEDBACK_FADE_MS = 300;
}
